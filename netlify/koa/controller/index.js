const init = require('./api/init')
const callback = require('./api/callback')
const collect = require('./api/collect')


module.exports = {
    init: init,
    callback: callback,
    collect: collect,
}